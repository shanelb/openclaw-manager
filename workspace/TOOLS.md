# TOOLS.md — Local Tool Notes

Optional notes about how *this* deployment uses tools (conventions, paths). This does **not** enable or disable tools — it only guides the model.

- Example: preferred shell patterns, where logs live, custom CLIs.

---

Add entries as your setup grows.
