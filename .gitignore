# Local-only secrets (never commit — use openclaw.json.example as a template)
.openclaw/openclaw.json

# Env and local overrides
.env
.env.*
!.env.example

# Agent daily memory pulled from VPS (private; do not commit)
workspace/memory/

# VPS workspace may include its own git + local state (not part of this repo)
workspace/.git/
workspace/.openclaw/
workspace/state/

# OS / editor
.DS_Store
*.swp
*~
